{
    "name": "regex-transform",
    "version": "0.0.1",
    "description": "Apply logical transformations to regexes.",
    "scripts": {
        "prepare": "husky",
        "pretty": "prettier --write .",
        "build": "tsc",
        "watch": "tsc -w",
        "test": "jest",
        "test:watch": "jest --watch"
    },
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "/dist"
    ],
    "devDependencies": {
        "@types/jest": "^29.5.13",
        "husky": "^9.1.6",
        "jest": "^29.7.0",
        "lint-staged": "^15.2.10",
        "nodemon": "^3.1.7",
        "prettier": "^3.3.3",
        "ts-jest": "^29.2.5",
        "typescript": "^5.6.2"
    },
    "lint-staged": {
        "*.{ts,js}": "prettier --write"
    },
    "dependencies": {
        "immutable": "^4.3.7"
    }
}
